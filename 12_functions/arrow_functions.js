let greet = function (name) {
  console.log(`Hello ${name}`);
};

greet("ahmed");

let greet_x = (namex) => {
  console.log(`Hello ${namex}`);
};

greet_x("mahmoud");

let square = (x) => x * x;
console.log(square(5));
